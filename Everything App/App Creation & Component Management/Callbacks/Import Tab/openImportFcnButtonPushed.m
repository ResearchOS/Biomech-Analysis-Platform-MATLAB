function []=openImportFcnButtonPushed(src)

fig=ancestor(src,'figure','toplevel');

% Get the current value of the data type drop down object

% If that file does not already exist, create it from the template.

% Open the file